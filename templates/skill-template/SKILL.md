---
name: ray-示例
description: 一句话说清这个 Skill 替用户完成什么；然后列出触发场景："帮我 XX""把 XX 做成 YY"时使用；最后写清边界——不做什么、不覆盖什么、不自动执行什么高影响动作。description 决定触发准确率，毕业前要过 benchmark。
---

# Ray 示例

一段话说明这个 Skill 的定位：输入是什么，产出是什么，人保留哪些判断权。

> **孵化状态**：写明当前处于什么阶段、已知缺陷、哪些场景还没验证。毕业时删除本段。

## 固定原则

1. 写下不可违反的硬约束（零覆盖 / 先预演 / 不自动发布……）。
2. 每条原则都应该是可检查的行为，不是态度表述。

## 1. 第一步

描述动作与判断。需要脚本时放进 `scripts/`，需要长篇背景放进 `references/`，SKILL.md 只保留流程本身。

## 2. 第二步

……

## 完成标准

- 列出可验证的完成条件，让"做完了"有客观定义。
- 用户只要求 A 时不得顺手做 B。
